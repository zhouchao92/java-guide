#### 死锁的四个必要条件？

四个条件**同时满足**才会死锁，破坏任意一个即可预防死锁：

- **互斥使用**：资源同一时刻只能被一个线程占用（如 synchronized 锁）
- **占有并等待**：线程已持有部分资源，仍阻塞等待其他线程持有的资源，且不释放已持有的
- **不可抢占**：线程持有的资源只能由自己释放，不能被其他线程强行夺走
- **循环等待**：存在线程互相等待的环形链，如 A 等 B、B 又等 A
